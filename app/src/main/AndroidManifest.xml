<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="acatictla.alan.thecheezery_133064">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".salties"></activity>
        <activity android:name=".registroActivity" />
        <activity android:name=".productosActivity" />
        <activity android:name=".product" />
        <activity android:name=".personalized" />
        <activity android:name=".paquetes" />
        <activity android:name=".menuActivity" />
        <activity android:name=".listView" />
        <activity android:name=".hotDrinks" />
        <activity android:name=".Dulcecitos" />
        <activity android:name=".combo" />
        <activity android:name=".coldDrinks" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>